{{- if not .isDesktopUser }}
home-sk-remote-sk\\x2dnc.service
default.target.wants/home-sk-remote-sk*
vdirsyncer.*
timers.target.wants/vdirsyncer.*
{{- end }}

{{- if not (and (eq .chezmoi.hostname "alien") .isDesktopUser) }}
kopia-alien-*
timers.target.wants/kopia-alien-*
{{- end }}

{{- if not (or .isDesktopEnv .isDevHost .isDevVm) }}
default.target.wants/atuin-*
sockets.target.wants/atuin-*
{{- end }}

{{- if not (and (eq .chezmoi.hostname "alien") (eq .chezmoi.username "sk")) }}
restic-backups-content-remote.*
restic-backups-user-data-remote.*
timer.target.wants/restic-backups-content-remote.*
timer.target.wants/restic-backups-user-data-remote.*
{{- end }}
