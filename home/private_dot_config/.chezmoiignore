{{- if or .isMinimalUser (not .isDesktopEnv) }}
khal
khard
mpv
vdirsyncer
{{- end }}
{{- if .isMinimalUser }}
rclone
{{- end }}

{{- if or .isMinimalUser (not .isDevHost) }}
zed
{{- end}}

{{- if not (and (eq .chezmoi.hostname "alien") (eq .chezmoi.username "sk")) }}
autostart/kopia-ui.desktop
kopia/*
{{- end }}
kopia/*.mlock
