{{- if not .isDesktopUser }}
autostart
{{- end }}

{{- if not (lookPath "atuin") }}
atuin
{{- end }}

{{- if or (not .isDesktopUser) (not (lookPath "cosmic-comp")) }}
cosmic
{{- end }}

{{- if not (lookPath "hx") }}
helix
{{- end }}

{{- if not (lookPath "htop") }}
htop
{{- end }}

{{- if or (not .isDesktopUser) (not (lookPath "khal")) }}
khal
{{- end }}

{{- if or (not .isDesktopUser) (not (lookPath "khard")) }}
khard
{{- end }}

{{- if or (not .isDesktopUser) (not (lookPath "lazygit")) }}
lazygit
{{- end }}

{{- if or (not .isDesktopUser) (not (lookPath "mpv")) }}
mpv
{{- end }}

{{- if or (not .isDesktopUser) (not (lookPath "opencode")) }}
opencode
{{- end }}

{{- if or (not .isDesktopUser) (not .isTrustedHost) (not (lookPath "rclone")) }}
rclone
{{- end }}

{{- if not (lookPath "rustic") }}
rustic
{{- end }}

{{- if not (lookPath "starship") }}
starship.toml
{{- end }}

{{- if or (not .isDesktopUser) (not (lookPath "task")) }}
task
{{- end }}

{{- if or (not .isDesktopUser) (not (lookPath "timew")) }}
timewarrior
{{- end }}

{{- if or (not .isDesktopUser) (not .isTrustedHost) (not (lookPath "vdirsyncer")) }}
vdirsyncer
{{- end }}

{{- if or (not .isNonRootUser) (not .isTrustedHost) (not (lookPath "zeditor")) }}
zed
{{- end}}
