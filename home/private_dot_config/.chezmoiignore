{{- if not .isDesktopUser }}
autostart
khal
khard
mpv
rclone
task
timewarrior
vdirsyncer
{{- end }}

{{- if not .isDesktopUser }}
cosmic
{{- end }}

{{- if or (not .isNonRootUser) (not .isDevHost) }}
zed
{{- end}}

{{- if not (and (eq .chezmoi.hostname "alien") (eq .chezmoi.username "sk")) }}
autostart/kopia-ui.desktop
kopia/*
{{- end }}
kopia/*.mlock
