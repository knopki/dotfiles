{{- if not (lookPath "git-lfs") }}
difftastic-config
{{- end }}

{{- if not (lookPath "git-lfs") }}
lfs-config
{{- end }}

{{- if not .isDesktopUser }}
sk-at-a-identity
sk-identity
wrk-config
{{- end }}
